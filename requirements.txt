# MAIC JSON Service with EyeLink Integration - Python 依赖包

# FastAPI 和 Web 服务相关
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP 客户端（用于测试和示例）
requests>=2.31.0

# 注意：pylink 需要单独安装 EyeLink Developers Kit
# pylink 不在 PyPI 上，需要从 SR Research 官方获取
# 安装 EyeLink Developers Kit 后，pylink 会自动可用
# 下载地址: https://www.sr-research.com/support/

# 可选依赖（如果需要）
# python-dotenv>=1.0.0  # 用于从 .env 文件加载环境变量
# aiofiles>=23.0.0      # 用于异步文件操作
